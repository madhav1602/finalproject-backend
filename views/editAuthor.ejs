<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Author</title>
    <style>
        /* Add your CSS styles here */
        body {
            font-family: 'Arial', sans-serif;
            margin: 20px;
        }

        h1 {
            color: #333;
        }

        form {
            width: 50%;
            margin: 0 auto;
        }

        label {
            display: block;
            margin-bottom: 5px;
        }

        input, select {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            box-sizing: border-box;
        }

        button {
            background-color: #4CAF50;
            color: white;
            padding: 10px;
            border: none;
            cursor: pointer;
        }

        a {
            text-decoration: none;
            color: #1E90FF;
            margin-right: 10px;
        }
    </style>
</head>
<body>
    <h1>Edit Author</h1>

    <form action="/editAuthor/<%= author.aid %>" method="post">
        <label for="aname">Author Name:</label>
        <input type="text" id="aname" name="aname" value="<%= author.aname %>" required>
        <br>

        <label for="email">Email:</label>
        <input type="email" id="email" name="email" value="<%= author.email %>" required>
        <br>

        <label for="password">Password:</label>
        <input type="password" id="password" name="password" value="<%= author.password %>" required>
        <br>

        <label for="dob">Date of Birth:</label>
        <input type="date" id="dob" name="dob" value="<%= author.dob ? author.dob.toISOString().split('T')[0] : '' %>">
        <br>

        <label for="dor">Date of Registration:</label>
        <input type="date" id="dor" name="dor" value="<%= author.dor ? author.dor.toISOString().split('T')[0] : '' %>">
        <br>

        <label for="phonenumber">Phone Number:</label>
        <input type="text" id="phonenumber" name="phonenumber" value="<%= author.phonenumber %>">
        <br>

        <label for="gender">Gender:</label>
        <select id="gender" name="gender">
            <option value="Male" <%= author.gender === 'Male' ? 'selected' : '' %>>Male</option>
            <option value="Female" <%= author.gender === 'Female' ? 'selected' : '' %>>Female</option>
            <option value="Other" <%= author.gender === 'Other' ? 'selected' : '' %>>Other</option>
        </select>
        <br>

        <label for="photourl">Photo URL:</label>
        <input type="text" id="photourl" name="photourl" value="<%= author.photourl %>">
        <br>

        <button type="submit">Update Author</button>
    </form>

    <br>
    <a href="/listAuthors">Back to Author List</a>
</body>
</html>
